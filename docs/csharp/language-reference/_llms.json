{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "Comprehensive reference for C# language syntax including keywords, operators, statements, types, and attributes. Documents reserved keywords like `class`, `async`, `await` and contextual keywords like `var`, `dynamic`, `record`. Use for precise syntax and semantic details."
}
