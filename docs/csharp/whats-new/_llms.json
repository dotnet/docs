{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "New features and improvements in each C# version. Recent additions include primary constructors (C# 12), collection expressions (C# 12), raw string literals (C# 11), and file-scoped types. Check version compatibility when using new features; target appropriate .NET version."
}
