{
  "title": "Networking",
  "description": "Making HTTP requests, working with sockets, and handling network protocols. Use `HttpClient` for REST APIs with `IHttpClientFactory` for proper lifecycle management. `System.Net.NetworkInformation` provides network availability monitoring; `System.Net.Sockets` enables low-level TCP/UDP communication.",
  "shortDescription": "HTTP clients, sockets, and network protocols."
}
