{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "Inspecting and invoking types, methods, and properties at runtime using the reflection API. Core types include `Type`, `MethodInfo`, `PropertyInfo`, and `Assembly`. Use reflection for dynamic type discovery, late binding, and custom attribute processing; consider source generators for AOT-compatible alternatives."
}
