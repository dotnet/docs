{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "API reference and usage guidance for .NET base class library types. Covers `System.*` namespaces including `System.Text`, `System.IO`, `System.Net`, and `System.Collections`. Provides detailed documentation for individual types, methods, and best practices."
}
