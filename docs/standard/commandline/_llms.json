{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "Building command-line applications with argument parsing, help generation, and tab completion. `System.CommandLine` provides a modern API for defining commands, options, and arguments with automatic validation. Used by the .NET CLI and many global tools."
}
