{
  "$schema": "https://example.com/llms-customization.schema.json",
  "description": "Working with data sources including SQLite databases and XML documents. `Microsoft.Data.Sqlite` provides a lightweight ADO.NET provider for SQLite; `System.Xml` namespaces support parsing, writing, XPath queries, XSLT transforms, and schema validation. For relational databases, also see Entity Framework Core."
}
