Option Explicit On
Option Strict On
Imports System.Reflection
Imports System.Windows.Forms
Imports System.IO
Imports NorthwindClient

Module Program
    Sub Main()
        'Dim newSync As New CustomerOrdersCustom()
        'newSync.Title = newSync.GetType().ToString()
        'newSync.ShowDialog()

        'Dim newAsync As New CustomerOrdersAsync()
        'newAsync.Title = newAsync.GetType().ToString
        'newAsync.ShowDialog()

        'Dim NewWpf As New CustomerOrdersWpf()
        'NewWpf.Title = NewWpf.GetType().ToString
        'NewWpf.ShowDialog()

        'Dim NewWpf2 As New CustomerOrdersWpf2()
        'NewWpf2.Title = NewWpf2.GetType().ToString
        'NewWpf2.ShowDialog()

        'Dim NewWpf3 As New CustomerOrdersWpf3()
        'NewWpf3.Title = NewWpf3.GetType().ToString
        'NewWpf3.ShowDialog()

        'Dim newSalesOrders As New SalesOrders()
        'newSalesOrders.Title = newSalesOrders.GetType().ToString
        'newSalesOrders.ShowDialog()

        'Dim newclientCredentials As New ClientCredentials()
        'newclientCredentials.Title = newclientCredentials.GetType().ToString
        'newclientCredentials.ShowDialog()

        'SourceVb.LinqWhereClause()
    End Sub
End Module
