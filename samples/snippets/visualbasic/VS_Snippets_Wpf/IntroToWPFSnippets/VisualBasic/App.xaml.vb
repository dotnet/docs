Namespace SDKSample

    Partial Public Class App
        Inherits Application

        Protected Overrides Sub OnStartup(ByVal e As System.Windows.StartupEventArgs)

            MyBase.OnStartup(e)

            'Dim window As New CodeWindow()
            'window.Show()

            'Dim window As New AnnotationsWindow()
            'window.Show()

            'Dim window As New ControlTemplateButtonWindow()
            'window.Show()

            'Dim window As New DataTemplateWindow()
            'window.Show()

            'Dim window As New DataTemplateWindowBefore()
            'window.Show()

            'Dim window As New DocumentViewerWindow()
            'window.Show()

            'Dim window As New EllipseEventHandlingWindow()
            'window.Show()

            'Dim window As New FlowDocumentReaderWindow()
            'window.Show()

            'Dim window As New LayoutWindow()
            'window.Show()

            'Dim window As New MarkupAndCodeBehindWindow()
            'window.Show()

            'Dim window As New MediaElementWindow()
            'window.Show()

            'Dim window As New ResourcesWindow()
            'window.Show()

            'Dim window As New StyleWindow()
            'window.Show()

            'Dim window As New TextBoxContentWindow()
            'window.Show()

            'Dim window As New TextDecorationsWindow()
            'window.Show()

            'Dim window As New ThemeWindow()
            'window.Show()

            Dim window As New UserControlWindow()
            window.Show()

        End Sub

    End Class

End Namespace